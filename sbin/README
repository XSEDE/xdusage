Workflow:

# Create TARs and RPMs
./sbin/build-packages

# Sign TARs and RPMs
./sbin/sign-packages

# Upload to development repository
./sbin/upload-packages

#uploads and signs the tarball
#./sbin/rpmupload
#uploads, signs, pushes rpm to all the repos, and updates all the repos
